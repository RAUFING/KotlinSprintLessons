package org.example


fun main() {
    val existing = 75
    val newOrders = 12
    val avgCheck = 2350.0
    val totalOrders = existing + newOrders
    val totalRevenue = totalOrders * avgCheck
    println(totalOrders)
    println(totalRevenue)
}